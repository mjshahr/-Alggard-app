// ⚠️ بعد از انتشار Google Sheet خودتان (File → Share → Publish to web → CSV)،
// لینک دریافتی را جای‌گزین مقدار زیر کنید.
// تا وقتی این مقدار خالی یا placeholder بماند، اپ با اطلاعات پیش‌فرض داخلی کار می‌کند.

const SHEET_CSV_URL = "PASTE_YOUR_PUBLISHED_CSV_LINK_HERE";
